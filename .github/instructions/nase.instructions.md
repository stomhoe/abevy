---
applyTo: '**'
---
cargo check, not build. avoid giving summarizations in chat. NEVER CREATE .md FILES. avoid creating code with redundancies, avoid defining an excessive amount of new components or resources just for trivial things. 
only if adequate: define a macro if many components share an extremely similar structure+impl (they are copy pastes of each other). afterwards, do a "cleaning pass" and read all the files you refactored and remove any leftover dead code, or redundancies. analyze the code to see if anything became redundant
