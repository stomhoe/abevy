id: dungeon_root
tags: [portal]
size: (1, 1)

let variant_a = +(fnl.tect, 0.0)
let variant_b = +(fnl.tect.s2, 0.0)

let a_diff = -(variant_a, variant_b)
let b_diff = -(variant_b, variant_a)

out = 0

[dublack] ""

